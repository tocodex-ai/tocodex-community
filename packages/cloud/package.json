{
	"name": "@roo-code/cloud",
	"description": "ToCodex Community local-only cloud compatibility stub.",
	"version": "0.0.0",
	"type": "module",
	"exports": "./src/index.ts",
	"scripts": {
		"lint": "eslint src --ext=ts --max-warnings=0",
		"check-types": "tsc --noEmit",
		"test": "node -e \"console.log('No public tests are currently shipped for ToCodex Community. Run pnpm check-types && pnpm build instead.')\"",
		"clean": "rimraf .turbo"
	},
	"dependencies": {
		"@roo-code/types": "workspace:^"
	},
	"devDependencies": {
		"@roo-code/config-eslint": "workspace:^",
		"@roo-code/config-typescript": "workspace:^",
		"@types/node": "^24.1.0",
		"@types/vscode": "^1.102.0",
		"globals": "^16.3.0",
		"vitest": "^3.2.4"
	}
}
